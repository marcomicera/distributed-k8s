flags:
  cloud: Kubernetes
  kubernetes_anti_affinity: false

mesh_network:
  name: mesh_network
  flag_zip_defs:
    default:
      vm_count: 7

#redis:
#  vm_groups:
#    default:
#      redis_clients: 6
